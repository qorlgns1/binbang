/**
 * Browser selector types only.
 * Types live in @workspace/shared so browser does not depend on runtime (rules: no browser → runtime).
 */
export type { PlatformSelectorCache, SelectorConfig } from '@workspace/shared/types';
