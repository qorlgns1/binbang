/**
 * Landing i18n: re-exports from config. Landing copy is loaded via next-intl (messages/landing.json).
 */
export type { Lang } from './config';
export { isValidLang, supportedLangs } from './config';
