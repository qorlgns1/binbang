// Re-export from @workspace/shared for backward compatibility
export { checkAccommodation, checkAirbnb, checkAgoda } from '@workspace/shared';
export type { AccommodationToCheck, CheckResult } from '@workspace/shared';
